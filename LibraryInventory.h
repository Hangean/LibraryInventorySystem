/******************************************************************************* LibraryStorage Class * * Represents the full library inventory system. * It contains multiple Shelf objects and manages operations across the * entire storage system. * * Main responsibility: * - control the collection of shelves * - add items to specific shelf/compartment locations * - check out and check in items * - print stored and checked-out items * - swap items between compartments * - support libraryStorage[shelf][compartment] access ******************************************************************************/#ifndef LIBRARY_INVENTORY_SYSTEM_SHELFSTORAGE_H#define LIBRARY_INVENTORY_SYSTEM_SHELFSTORAGE_H#include <vector>#include <memory>#include <string>#include "Item.h"#include "Shelf.h"class LibraryInventory {private:    int MAX_SHELF_SIZE = 5;    vector<Shelf> shelves;public:    LibraryInventory() : shelves(MAX_SHELF_SIZE){}    /**************************************************************************    * Return Shelf& so libraryInventory[2][4] works.     *     * If this returned Shelf*, then to becomes to ptr[4], it would mean     * pointer arithmetic, not compartment access.    ***************************************************************************/    Shelf &operator[] (int index);    void addItem(int shelf, int compartment, unique_ptr<Item> item);    void removeItem(int shelf, int compartment);    void checkOutItem(string id, string &borrowName, string &dueDate);    void checkInItem(string id);    void printStoredItems();    void printCheckOutItems();    void swapItems(int shelf1, int compartment1,                   int shelf2, int compartment2);};#endif //LIBRARY_INVENTORY_SYSTEM_SHELFSTORAGE_H