/******************************************************************************* Item Class * * The Item class is the base class for all library items. It stores the common * information shared by every item, including name, description, and unique ID. * * Book, Movie, and Magazine will inherit from this class and add their own * specific data. * * This class supports polymorphism through virtual functions, allowing different * item types to be handled through Item pointers. ******************************************************************************/#ifndef LIBRARY_INVENTORY_SYSTEM_ITEM_H#define LIBRARY_INVENTORY_SYSTEM_ITEM_H#include <string>#include <ostream>using namespace std;class Item {private:    string name;    string description;    string id;    bool checkedOut;    string borrowName;    string dueDate;public:    Item()        : name(""),          description(""),          id(""),          checkedOut(false),          borrowName(""),          dueDate("") {    }    Item(const std::string &name,         const std::string &description,         const std::string &id)        : name(name),          description(description),          id(id),          checkedOut(false),          borrowName(""),          dueDate("") {    }    virtual ~Item() = default;    void setName(const string &name);    void setDescription(const string &descriptions);    void setId(const string &id);    bool isCheckedOut() const;    void checkOut(const string &borrowName, const string &dueDate);    void checkIn();    string getName() const;    string getDescription() const;    string getId() const;    string getBorrowName() const;    string getDueDate() const;    virtual void print(ostream &out) const = 0;};ostream &operator<<(ostream &out, const Item &item);#endif //LIBRARY_INVENTORY_SYSTEM_ITEM_H