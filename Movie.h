/******************************************************************************* Movie Class * * Represents a movie item in the library inventory system. * Movie is derived from the Item base class, so it already has the common * item information: name, description, and id. * * Additional movie-specific data: * - title * - director * - mainActors * * Main responsibility: * - store movie-specific information * - override the print function from Item * - allow Movie objects to be handled polymorphically through Item pointers ******************************************************************************/#ifndef LIBRARY_INVENTORY_SYSTEM_MOVIE_H#define LIBRARY_INVENTORY_SYSTEM_MOVIE_H#include <string>#include <vector>#include <iostream>#include "Item.h"using namespace std;class Movie : public Item {private:    string title;    string director;    vector<string> mainActors;public:    Movie()        : Item(),          title(""),          director(""),          mainActors() {    }    Movie(const string &name,          const string &description,          const string &id,          const string &title,          const string &director,          const vector<string> &mainActors)        : Item(name, description, id),          title(title),          director(director),          mainActors(mainActors) {    }    string getTitle() const;    string getDirector() const;    vector<string> getMainActors() const;    void setTitle(string title);    void setDirector(string director);    void setMainActors(vector<string> mainActors);    void addMainActor(string actor);    void print(ostream &out) const override;};#endif // LIBRARY_INVENTORY_SYSTEM_MOVIE_H