/******************************************************************************* Book Class * * Represents a book item in the library inventory system. * Book is derived from the Item base class, so it already has the common * item information: name, description, and id. * * Additional book-specific data: * - title * - author * - copyrightDate * * Main responsibility: * - store book-specific information * - override the print function from Item * - allow Book objects to be handled polymorphically through Item pointers ******************************************************************************/#ifndef LIBRARY_INVENTORY_SYSTEM_BOOK_H#define LIBRARY_INVENTORY_SYSTEM_BOOK_H#include <string>#include <iostream>#include "Item.h"using namespace std;class Book : public Item {private:    string title;    string author;    string copyrightDate;public:    Book() : Item(), title(""), author(""), copyrightDate("") {    }    Book(const string &name,         const string &description,         const string &id,         const string &title,         const string &author,         const string &copyrightDate)        : Item(name, description, id),          title(title),          author(author),          copyrightDate(copyrightDate) {    }    string getTitle() const;    string getAuthor() const;    string getCopyrightDate() const;    void setTitle(string title);    void setAuthor(string author);    void setCopyrightDate(string copyrightDate);    void print(ostream &out) const override;};#endif //LIBRARY_INVENTORY_SYSTEM_BOOK_H