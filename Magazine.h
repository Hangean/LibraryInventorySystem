/******************************************************************************* Magazine Class * * Represents a magazine item in the library inventory system. * Magazine is derived from the Item base class, so it already has the common * item information: name, description, and id. * * Additional magazine-specific data: * - edition * - mainArticleTitle * * Main responsibility: * - store magazine-specific information * - override the print function from Item * - allow Magazine objects to be handled polymorphically through Item pointers ******************************************************************************/#ifndef LIBRARY_INVENTORY_SYSTEM_MAGAZINE_H#define LIBRARY_INVENTORY_SYSTEM_MAGAZINE_H#include <string>#include <iostream>#include "Item.h"using namespace std;class Magazine : public Item {private:    string edition;    string mainArticleTitle;public:    Magazine()        : Item(),          edition(""),          mainArticleTitle("") {    }    Magazine(const string& name,             const string& description,             const string& id,             const string& edition,             const string& mainArticleTitle)        : Item(name, description, id),          edition(edition),          mainArticleTitle(mainArticleTitle) {    }    string getEdition() const;    string getMainArticleTitle() const;    void setEdition(string edition);    void setMainArticleTitle(string mainArticleTitle);    void print(ostream& out) const override;};#endif // LIBRARY_INVENTORY_SYSTEM_MAGAZINE_H